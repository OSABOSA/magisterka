apiVersion: apps/v1
kind: Deployment
metadata:
  name: cpu-service
  namespace: magisterka
  labels:
    app: cpu-service
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: cpu-service
  template:
    metadata:
      labels:
        app: cpu-service
        tier: backend
    spec:
      # --- POPRAWKA 1: Wymuszenie instalacji na apps-pool ---
      nodeSelector:
        workload: apps
      containers:
        - name: cpu-service
          # --- POPRAWKA 2: Ścieżka do Artifact Registry w GCP (Zmień TWÓJ_ID_PROJEKTU) ---
          image: europe-west3-docker.pkg.dev/magisterka-497513/magisterka-repo/cpu-service:latest
          # Usunęliśmy IfNotPresent, bo GKE musi zawsze pobrać nowy obraz z sieci GCP
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: 1000m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10