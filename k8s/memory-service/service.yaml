apiVersion: v1
kind: Service
metadata:
  name: memory-service
  namespace: magisterka
  labels:
    app: memory-service
spec:
  type: ClusterIP
  selector:
    app: memory-service
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
