apiVersion: v1
kind: Service
metadata:
  name: echo-service
  namespace: magisterka
  labels:
    app: echo-service
spec:
  type: ClusterIP
  selector:
    app: echo-service
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
