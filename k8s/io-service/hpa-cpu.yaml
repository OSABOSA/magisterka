apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: io-service-hpa
  namespace: magisterka
  labels:
    app: io-service
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: io-service
  minReplicas: 1
  maxReplicas: 30
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 50
