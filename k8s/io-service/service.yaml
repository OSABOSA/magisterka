apiVersion: v1
kind: Service
metadata:
  name: io-service
  namespace: magisterka
  labels:
    app: io-service
spec:
  type: ClusterIP
  selector:
    app: io-service
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
